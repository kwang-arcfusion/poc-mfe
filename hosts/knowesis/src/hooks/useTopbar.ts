// hosts/knowesis/src/hooks/useTopbar.ts
import { useTopbarStore } from '../stores/topbarStore';

// Export store hook ออกไปภายใต้ชื่อ useTopbar เพื่อให้เรียกใช้งานได้ง่าย






export const useTopbar = useTopbarStore;
