{
  "name": "arcfusion",
  "private": true,
  "scripts": {
    "build": "pnpm clean:p && turbo run build",
    "b:p": "cd packages/client && rm -rf .turbo dist tsconfig.tsbuildinfo && pnpm build && cd ../store && rm -rf .turbo dist tsconfig.tsbuildinfo && pnpm build && cd ../ui && rm -rf .turbo dist tsconfig.tsbuildinfo && pnpm build",
    "clean:p": "rm -rf packages/*/tsconfig.tsbuildinfo packages/*/.turbo packages/*/dist",
    "b:ui": "pnpm build --filter=@arcfusion/ui",
    "b:store": "pnpm build --filter=@arcfusion/store",
    "b:types": "pnpm build --filter=@arcfusion/types",
    "b:client": "pnpm build --filter=@arcfusion/client",
    "d:knowesis": "turbo run dev --filter={hosts/knowesis}...",
    "dev:all": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "clean": "rm -rf node_modules .turbo hosts/*/dist remotes/*/dist packages/*/dist",
    "s:knowesis": "serve -s -p 8000 hosts/knowesis/dist",
    "s:ask_ai": "serve -s -p 8001 remotes/ask_ai/dist",
    "s:home": "serve -s -p 8002 remotes/home/dist",
    "s:stories": "serve -s -p 8003 remotes/stories/dist",
    "s:overview": "serve -s -p 8004 remotes/overview/dist",
    "preview": "run-p s:*"
  },
  "overrides": {
    "@babel/preset-typescript": "7.27.1"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.1",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
